import { useState } from 'react';

const FilterBar = () => {
    // const projectData = useProject();
    // const [projectSelected, setProjectSelected] = useState<Iproject>();

    // const projectBox = (value:Iproject, index:number) => {
    //     return (
    //         <StyledBox key={index} onClick={() => setProjectSelected(value)}>
    //             <p>{value.id}</p>
    //             <p>{value.name}</p>
    //             <p>{value.created_on}</p>
    //         </StyledBox>
    //     )
    // }

    return <p>Search bar</p>
}

export default FilterBar;